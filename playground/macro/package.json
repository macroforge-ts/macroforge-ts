{
	"name": "@playground/macro",
	"version": "0.0.1",
	"private": true,
	"main": "index.js",
	"types": "index.d.ts",
	"napi": {
		"binaryName": "playground-macro",
		"triples": {}
	},
	"files": [
		"index.d.ts",
		"index.js"
	],
	"scripts": {
		"build": "napi build --platform --release -p playground-macros",
		"prepare": "npm run build",
		"clean": "rm -rf *.node",
		"cleanbuild": "rm -rf *.node && napi build --platform --release -p playground-macros"
	},
	"engines": {
		"node": ">= 18"
	}
}
