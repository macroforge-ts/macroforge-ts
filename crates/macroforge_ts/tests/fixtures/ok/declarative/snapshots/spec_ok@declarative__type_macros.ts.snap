---
source: macroforge_ts/tests/spec_tests.rs
expression: snapshot
input_file: macroforge_ts/tests/fixtures/ok/declarative/type_macros.ts
---
## Input

import { macroRules } from "macroforge/rules";

// Type-position macro: wrap a type in a readonly mapped type.
const $ReadOnly = macroRules({
  kind: "type",
  expand: macroRules`
    ($t:Type) => { readonly [K in keyof $t]: $t[K] }
  `,
});

// Type-position macro with repetition: build a tuple of types.
const $Tuple = macroRules({
  kind: "type",
  expand: macroRules`
    ($($t:Type),+) => [$($t),+]
  `,
});

interface User {
  id: string;
  name: string;
}

type StrictUser = $ReadOnly<User>;
type Pair = $Tuple<string, number>;
type Triple = $Tuple<string, number, boolean>;

## Output

// Type-position macro: wrap a type in a readonly mapped type.


// Type-position macro with repetition: build a tuple of types.


interface User {
  id: string;
  name: string;
}

type StrictUser = { readonly [K in keyof User]: User[K] };
type Pair = [string,number];
type Triple = [string,number,boolean];

## DTS

(none)

## Diagnostics

(none)
